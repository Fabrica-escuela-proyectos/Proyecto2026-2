version: '3.8'
services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_URL=jdbc:postgresql://postgres:reservas-backend@db.sruzcycwcedbylqgoayk.supabase.co:5432/postgres
      - DB_USERNAME=postgres
      - DB_PASSWORD=reservas-backend
      - JWT_SECRET=${JWT_SECRET}
      - SPRING_PROFILES_ACTIVE=dev